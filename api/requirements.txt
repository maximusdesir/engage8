# Engage Eight API dependencies (Python 3.11 to 3.14)
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.1
python-jose[cryptography]>=3.3   # JWT
bcrypt>=4.1                     # password hashing
python-multipart>=0.0.9          # file uploads
email-validator>=2.0           # EmailStr validation

# The prediction endpoints reuse the trained model in ../ml, so the API
# environment also needs the ML stack:
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
